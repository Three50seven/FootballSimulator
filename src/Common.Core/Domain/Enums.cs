namespace Common.Core
{
    public enum CommandTypeOption
    {
        Added = 1,
        Updated,
        Deleted
    }

    public enum SortDirectionOption
    {
        Ascending,
        Descending
    }
}
